import { style } from "@vanilla-extract/css";
import { vars } from "@styles/theme.css";

export const feedDetailsPageLayout = style({
  backgroundColor: vars.colors.backgroundColor,
  width: "100%",
  height: "100%",
});

export const commentContainer = style({
  backgroundColor: vars.colors.backgroundColor,
});
